/**
 * Nano Motion demo configuration.
 *
 * Replace GTM-XXXXXXX with your own Google Tag Manager container ID.
 * Keeping the placeholder is safe: the site and event inspector still work,
 * but no external GTM script is loaded.
 */
window.NANO_MOTION_CONFIG = {
  gtmContainerId: "GTM-T7JBQK6P",
  currency: "GBP",
  locale: "en-GB",
  demoMode: true
};
